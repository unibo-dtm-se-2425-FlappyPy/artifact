import FlappyPy

# this is the main module of your app
# it is only required if your project must be runnable
# this is the script to be executed whenever some users writes `python -m FlappyPy` on the command line, eg.
x = FlappyPy.MyClass().my_method()
print(x)
